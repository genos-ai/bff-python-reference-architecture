# Conda environment for BFF Web Skeleton.
#
# Create and activate:
#   conda env create -f environment.yml
#   conda activate bff_web
#
# Then install project dependencies:
#   pip install -r requirements.txt

name: bff-web-skeleton
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
